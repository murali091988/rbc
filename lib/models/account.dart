import 'package:flutter/material.dart';

class Account {
  final String title, subTitle;
  final Icon menuIcons;
  final int menuType;

  Account(this.title, this.subTitle, this.menuIcons, this.menuType);
}
